# Optional provider verification. These stay server-side and are never exposed to the browser.
# Leave empty if not using API verification features.
GEMINI_API_KEY=
ANTHROPIC_API_KEY=

# Site URL (required for production)
NEXT_PUBLIC_SITE_URL=http://localhost:3000
