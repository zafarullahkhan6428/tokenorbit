import Header from '@/components/Header';
import { models } from '@/data/models';
import { seoPages } from '@/app/seo-pages';
export const metadata={title:'AI Token & Cost Tools',description:'Free AI token, cost, context and model comparison tools from TokenOrbit.',alternates:{canonical:'/tools'}};
export default function Tools(){return <><Header/><main className="container section"><nav className="breadcrumbs" aria-label="Breadcrumb"><a href="/">Home</a><span className="crumb-sep">/</span><span>Tools</span></nav><div className="eyebrow">TokenOrbit toolkit</div><h1 style={{fontSize:52}}>AI token and cost tools</h1><p className="section-intro">Free utilities for estimating token usage, AI API costs, context-window usage and recurring LLM workloads.</p><div className="grid">{seoPages.map(p=><a className="card" href={`/tools/${p.slug}`} key={p.slug}><div className="badge">Free tool</div><h3 style={{marginTop:10}}>{p.title}</h3><p>{p.description}</p></a>)}<a className="card" href="/tools/model-cost-comparison"><div className="badge">Comparison</div><h3 style={{marginTop:10}}>Model Cost Comparison</h3><p>Compare one representative prompt across configured pricing profiles.</p></a></div><div className="card content-block"><h2>Model data and accuracy</h2><p>TokenOrbit labels estimates and provider-backed counts separately. Model pricing is maintained in a centralized registry so it can be reviewed and updated without changing calculator code.</p><p><a className="text-link" href="/models">Browse model profiles →</a> · <a className="text-link" href="/methodology">Read the methodology →</a></p><p className="muted">{models.length} model profiles are currently configured.</p></div></main></>}
